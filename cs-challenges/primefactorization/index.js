/**
 * Returns an array of prime factors of a given number
 * Any composite number can be broken down into `k` number of prime factors
 * i.e., 12 is not prime but can be represented as 2 * 2 * 3 = 12
 * 19 is already prime so its prime factor is just 19
 *
 * @param {Number} n - Integer to get the prime factors from
 *
 * @return {Array} - Array of prime factors
 */
const primeFactor = (n) => {
  // TODO: implement
};

module.exports = primeFactor;
